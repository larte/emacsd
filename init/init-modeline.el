;; Telephone line
(package-initialize)
(require-package 'powerline)

(require 'cl)

(use-package powerline
             :init

             (powerline-center-theme)
             )


(provide 'init-modeline)
